#include <iostream>

using namespace std;

int t,a,b,c,d;

int main(){
    cin.sync_with_stdio(false);
    cin >> t;
    while(t--){
        cin >> a >> b >> c >> d;
        cout << b << ' ' << c << ' ' << c<< endl;
    }
    return 0;
}